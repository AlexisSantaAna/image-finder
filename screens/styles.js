import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  startContainer: {
    flex: 1,
    alignItems: "center",
    paddingVertical: 45,
  },
  startText: {
    fontSize: 18,
    fontFamily: "Poppins-Regular",
    paddingBottom: 10,
  }
});
